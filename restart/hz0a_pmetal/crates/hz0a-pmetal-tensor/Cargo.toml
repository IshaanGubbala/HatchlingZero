[package]
name = "hz0a-pmetal-tensor"
edition.workspace = true
license.workspace = true
version.workspace = true
authors.workspace = true

[dependencies]
hz0a-pmetal-kernel = { path = "../hz0a-pmetal-kernel" }

[dev-dependencies]
# Test-only, does not affect the zero-dependency production crate: used
# solely to parse the Python-generated cross-language parity fixture.
serde_json = "1"
