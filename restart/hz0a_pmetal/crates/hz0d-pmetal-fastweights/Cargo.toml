[package]
name = "hz0d-pmetal-fastweights"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]

[dev-dependencies]
# Test-only, mirrors hz0b-pmetal-memory's own convention: parses the
# Python-generated cross-language parity fixture, does not affect the
# zero-dependency production crate.
serde_json = "1"

[lib]
path = "src/lib.rs"
