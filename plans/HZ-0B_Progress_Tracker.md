# HZ-0B Progress Tracker

Updated: August 1, 2026 (B11 scientific verdict: HZ-0B not validated)

## Mission

Add explicit, controllable, session-local associative memory to a finished and frozen HZ-0A base.

## Current Status (honest verdict, 2026-08-01)

**HZ-0B B11 failed under multi-seed, matched-capacity, real-checkpoint
evaluation. The current learned write mechanism is unresolved. HZ-0B
overall is NOT validated -- no capability claim should be made from this
project as it stands.**

- **B1-B9: engineering and integration complete, verified.** The memory
  contract, pure simulator, semantics layer, baselines, read-only
  integration, controlled writes, latent writes, and fine-tuning
  compatibility are all real, tested, and correctly wired to the frozen
  HZ-0A checkpoint. This is genuine evidence the SYSTEM is implemented
  coherently -- it is not evidence the memory MECHANISM is useful. Those
  are different claims and this tracker previously conflated "the plumbing
  works" with "the capability exists"; corrected here.
- **B10: CPU-tensor Rust port and Python bridge complete and tested**
  (real, parity-verified against the Python reference); the GPU tier was
  deliberately not built, benchmark-justified. Infrastructure-complete,
  independent of B11's finding.
- **B11: the phase that actually tests the plan's central claim ("HZ-0B
  provides a measurable advantage that cannot be explained only by more
  parameters or more context") has run, and the claim is not supported.**
  Real memory scored WORSE than a matched-parameter no-memory adapter and
  worse than chance under proper multi-seed, realistic-scale evaluation on
  the real frozen checkpoint (`docs/restart/hz0b_b11_evaluation_results.md`).
  Three targeted fixes (more training steps, more memory slots, hard/
  discrete write decisions via STE) each helped a little or not at all,
  none closed the gap -- ruling out undertraining, slot-capacity
  starvation, and soft-write blur as the sole explanations. The remaining
  likely causes are deeper: unstable credit assignment into writes,
  destructive/high-variance writes, a read path that outpaces the write
  path during training, backbone representations that were never trained
  to be "write-friendly," and/or an end-task loss too indirect to
  supervise a semi-discrete write decision.
- **Write-mechanism diagnosis started, factorial cell 1 run (2026-08-01):
  the problem is NOT confined to the learned write controller.** Cell 1
  (oracle timing + oracle content + oracle slot -- everything handed to
  the mechanism for free, zero learned write component) still only
  reached mean 0.306 (std 0.115, range 0.125-0.438, 5 seeds) -- below the
  equal-param adapter's 0.512, still near chance
  (`docs/restart/hz0b_b11_evaluation_results.md`, "B11 continuation:
  write-mechanism diagnosis"). This directly implicates the READ path
  itself (`gated_memory_read`'s query/gate/value-to-hidden mechanism),
  not just the write controller -- the same read mechanism that reached a
  perfect rank-0 result in B6, but on an easier, single-fixed-fact task
  that (per B8 Stage 3's own critique) never actually tested genuine
  content discrimination. Remaining factorial cells (oracle timing +
  learned content; learned timing + oracle content; learned slot choice)
  not yet run -- named as the concrete next step, not chased further this
  pass. Do not resume full B11 evaluation, and do not proceed to HZ-0C
  describing HZ-0B as complete, until the reopening criteria below are
  met.
- Working assumption: all legacy HZ-0B behavior must be re-audited before
  reuse -- **this was not a hypothetical caution**, see B0 below. The
  same discipline now applies to this project's OWN B11 single-seed
  result, which also turned out not to hold up.

### Reopening criteria for HZ-0B (do not resume full B11 training until ALL of these pass)

1. ~~Oracle-everything memory (fixed correct content, fixed correct
   timing, fixed correct slot) decisively beats no-memory on the SAME
   task/scale B11's reversal was found on.~~ **NOT MET, checked directly
   (2026-08-01): oracle-everything scored mean 0.306, below the
   equal-param adapter's 0.512 and near chance.** This criterion cannot
   be satisfied by write-controller fixes alone -- the read path itself
   needs to be diagnosed next.
2. Learned-component ablations (the factorial matrix below) identify one
   tractable bottleneck, not a diffuse failure across every component.
3. Writes measurably improve retrieval BEFORE any full language-model
   fine-tuning is layered on top.
4. Results are multi-seed stable (this project's own hard-won lesson,
   now twice-confirmed: once via GDN-3, once via B11 itself).
5. Memory state does not degrade monotonically as write count grows.
6. Bad writes can be detected or rolled back, not just hoped away.

## Phase Tracker

**Correction (2026-07-29): this table previously used a compressed, 9-phase (B0-B8) numbering that did not match `plans/HZ-0B_Total_Restart_Plan.md`'s actual 12-phase (B0-B11) structure** -- e.g. this table's old "B4: training curriculum" is really the plan's B8, and the plan's actual B4 ("fair baselines") had no row at all. All work done this session was against the real plan document directly, so the completed-phase content below is correct; only the table's own numbering/row set was wrong, now fixed to match the plan exactly.

| Phase | Deliverable | Status | Notes |
| --- | --- | --- | --- |
| B0 | Git-history audit | **Complete** (both required deliverables) | `docs/restart/hz0b_history_audit.md` + `docs/restart/hz0b_recovered_requirements.md`. Full `git log --all` sweep (not just the checked-out archive/ tree) found ~40 commits, almost all within one 11-hour burst on 2026-07-26, with three separate "complete"/"production ready" declarations inside a single 9-minute window (22:14-22:23) -- claim density alone is evidence against trusting them. Checked directly against raw probe data: all four gate probes (`memory-probe-*-step425.json`, plus earlier v1/v2 checkpoints) show `0.0` recall before and after fine-tuning on the real 110M-scale system, every time it was measured. The one narrower positive number (`PRODUCTION_READY.md`'s "36M backbone, 90% recall") has no raw result file anywhere in the tree and conflates training recall with held-out recall on the single easiest curriculum stage. `mem-fix-plan-2026-07-26.md` -- corrected from the first audit pass -- is not a same-day note but a **retrospective correction**, committed the next day (2026-07-27) as part of the same reorg that created this tracker; it independently reaches the same 0.0 conclusion this audit's own raw-JSON read reached, a genuine cross-confirmation. Recovered requirements doc extracts the real read/write/gating equations from `hybrid_lm.py`/`session_scratchpad.py` (cited to file:line, not assumed) and finds the legacy code implements only 3 of the 10 operations B1 requires (`read`/`write`/`reset`; no `reinforce`/`update`/`protect`/`forget`/`delete`/`serialize`/`restore`), plus a real lifecycle scope gap (legacy memory resets every `forward()` call -- it is sequence-local, not cross-call session-persistent, despite the "session" framing). |
| B1 | define the memory contract | **Complete** | `docs/restart/hz0b_b1_memory_contract.md`. Six state tensors (`keys`, `values`, `confidence`, `age`, `protection_strength`, `write_metadata`), ten operations with real signatures, all 14 required design decisions made with stated rationale. Key deviation from legacy, motivated by the B0 audit: memory is now genuinely content-addressable (`keys` is real per-slot state, written and matched against) instead of legacy's fixed learned `slot_addresses` routing parameter that never connected to what was actually stored -- a direct, targeted response to B0's still-open "was hard routing itself the broken link" question. `reinforce`/`update`/`protect` kept as three separate operations (legacy collapsed all of this into one `momentum` knob, which its own config comments show was an active footgun). Soft addressing decided first, hard/STE routing deferred to a later explicitly-gated experiment rather than repeating legacy's unverified jump straight to it. Session lifecycle scoped honestly to sequence-local (matching what legacy code actually did, not the more ambitious "session" framing in the plan's Objective) with cross-call persistence named as explicit future work, not assumed. |
| B2 | pure reference simulator | **Complete** | `reference/hz0b_memory_simulator.py` + `tests/reference/test_hz0b_memory_simulator.py`. All 14 of the plan's own listed B2 tasks pass (store/retrieve exact+noisy/multiple keys/similar keys/overwrite/reinforce/protect/overwrite-different/forget-by-age/overflow/reset+restore/chained retrieval/conflicting writes), plus delete, determinism, and learned-mode differentiability checks -- 20 tests total. Found 3 real bugs via the test run (not assumed correct on first write): a write-collision case that let protected memories get silently duplicated instead of actually protected, a non-differentiable similarity normalization that produced NaN gradients at empty (all-zero) slots, and a flawed test fixture that accidentally created near-duplicate keys. All fixed and re-verified. Exit gate ("the simulator passes predefined tests without any language model attached") satisfied -- no LM anywhere in the file. This is also the plan's own "first concrete milestone." |
| B3 | solve memory semantics | **Complete** | `reference/hz0b_memory_semantics.py` + `tests/reference/test_hz0b_memory_semantics.py`. Directly answers the plan's own critique of B2 ("Do not rely on one similarity threshold for all behaviors" -- B2's write() used a single 0.95 cutoff for everything): separate, named thresholds (update-match 0.9, interference 0.6, staleness half-life) and graded (sigmoid) control signals -- write/update/reinforce/contradiction probability, erase_strength, soft slot_selection_distribution, memory_validity_confidence distinct from raw occupancy confidence -- plus all 5 required penalty functions (excessive writes, uncontrolled growth, protected-memory corruption, duplicate memories, unstable norms) as pure, independently-tested functions. 11/11 tests verify the exit gate directly: new writes, reinforcement, updates, deletion, and protection each produce measurably different signal values. Found a real test-fixture bug along the way (a "near-duplicate" test key was accidentally similar enough to trigger B2's own write-match threshold) and fixed it. |
| B4 | fair baselines before integration | **Complete** | `reference/hz0b_baselines.py` + `tests/reference/test_hz0b_baselines.py`. All 6 required baselines implemented, each deliberately weaker than HZ-0B in one named way: no-memory (pure control), larger recurrent state (raw capacity, no addressing -- blends writes together), longer context (unbounded soft attention over full history), simple KV cache (exact-match only, no similarity fallback), external vector retrieval (unbounded, hard top-1 nearest-neighbor), equal-parameter feed-forward adapter (same parameter ballpark, no memory state at all). 7/7 tests verify the exit gate directly ("the evaluation harness can compare HZ-0B against simpler alternatives") by running the same exact/noisy-recall task across baselines and confirming they do NOT pass/fail uniformly. |
| B5 | wait for a stable HZ-0A checkpoint | **Complete (2026-07-30)** | `docs/restart/hz0b_b5_readiness_check.md` mapped 5 of 7 items ahead of time (reproducible tokenizer, stable PMetal implementation, verified gradients, reliable checkpoint loading). The remaining 2 closed together: Stage 2 finished for both architectures (hybrid 2026-07-30, transformer 2026-07-29), and the full-holdout eval (`scripts/hz0a_select_best_full_holdout.py`) completed cleanly for both, solo, no contention -- see `plans/HZ-0A_Progress_Tracker.md`'s full-holdout comparison table. "A trained checkpoint" is satisfied (both architectures, all milestones, best-full-holdout snapshots saved). "A known no-memory evaluation baseline" is satisfied by construction -- HZ-0A's own full-holdout numbers (hybrid 2.441210, transformer 2.586484 at 100M tokens) *are* the no-memory baseline, exactly as the readiness doc anticipated. HZ-0A's architecture is now explicitly frozen (`plans/HZ-0A_Progress_Tracker.md`, "Both Stage 2 architectures are now complete"). All 7 items satisfied -- B6 unblocked. |
| B6 | read-only integration with frozen HZ-0A | **Real integration done; exit gate partially satisfied, honestly** | `reference/hz0b_b6_hz0a_integration.py` (bridges the isolated `hz0b_readonly_integration.py` module to the real, frozen `HZ0AMlxModel` without modifying it -- reproduces its embedding->blocks->final_norm->LM-head path externally with an optional memory-read inserted before `final_norm`), `scripts/hz0b_b6_real_integration_probe.py`, `tests/reference/test_hz0b_b6_real_integration.py`, `docs/restart/hz0b_b6_real_integration_results.md`. Two real results against the actual best-full-holdout hybrid checkpoint: (1) empty memory is bit-identical to no memory on real held-out data (free, deterministic, now a permanent regression test) -- confirms the isolated-test finding transfers exactly; (2) training ONLY the read-path projections (query/gate/value-to-hidden, backbone frozen, memory populated via the oracle/non-learned bypass per B1 decision 13, no write controller involved) against a synthetic recall task moved the target token's rank from 5378th to **0th** (top prediction) on held-out, unseen-prefix prompts -- genuine generalization, not memorization. But general held-out cross-entropy on unrelated real text rose **+2.54%** with the same trained projections and oracle memory populated -- a real, disclosed, NOT-explained-away cost, not rounded up to "no degradation." B6's exit gate ("improves memory-specific tasks without materially degrading general held-out loss") is honestly only partially met. **Tuned (2026-07-30):** added a background-preservation regularizer (`--lambda-preserve`, an auxiliary loss on a real, disjoint slice of held-out text, oracle memory populated but no trigger present -- directly penalizes the trained read path for firing where it shouldn't) -- a 4-point sweep (lambda 0/1/5/20) found lambda=5 keeps the task fully solved (rank 0) while cutting general degradation from +2.54% to **+1.40%** (~45% relative reduction), non-monotonically (lambda=20 was worse, not better). Real improvement, not a full resolution -- degradation is smaller, not zero; see `docs/restart/hz0b_b6_real_integration_results.md` section 3 for the full sweep and caveats. **Structural fix (2026-07-30, same date):** added `confidence_scaled` to `gated_memory_read` -- the gate is also multiplied by the read's own retrieval confidence, so a trained bias term can no longer leak through on empty/irrelevant memory (the exact mechanism traced while building B7, see that entry below). Combined with `lambda_preserve=5`: degradation drops from +1.40% to **+0.38%** (task still fully solved, rank 0) -- an ~85% relative reduction from the original untuned +2.54%. Default `confidence_scaled=False` everywhere, so nothing already reported changed underneath it; this is opt-in. New deterministic regression tests in `tests/reference/test_hz0b_confidence_scaled_gate.py` (4/4) lock in the fix (reproduce the bug with `confidence_scaled=False`, prove it's gone with `True`, confirm real reads still work, confirm differentiability). |
| B7 | controlled writes | **Real integration done; exit gate partially satisfied, honestly** | Isolated prep: `reference/hz0b_write_integration.py` + `tests/reference/test_hz0b_write_integration.py` (5/5 tests; found and fixed a real correctness gap along the way -- see prior entry, preserved below). Real integration: `reference/hz0b_b7_hz0a_integration.py` (sequential per-position memory loop against real frozen hidden states -- a write triggered by a trained gate at one position is what a read at a LATER position in the SAME forward pass retrieves, not an external oracle bypass like B6), `scripts/hz0b_b7_real_integration_probe.py`, `tests/reference/test_hz0b_b7_real_integration.py` (3/3 deterministic wiring tests), `docs/restart/hz0b_b7_real_integration_results.md`. Core result: training only the controller projections (backbone frozen, oracle key/value fixed, write gate learns WHEN to write from `should_write` supervision) moved the target token's rank from 6944.8 (read-only floor, memory never written) to **179.4** on held-out unseen prefixes -- a real store-then-retrieve demonstration inside one forward pass, though not as clean as B6's rank-0 result (B7's task didn't fully converge in the same step budget). **Real finding along the way**: traced the EXACT mechanism behind B6's residual general-degradation -- `gated_memory_read`'s learned bias term (`value_to_hidden_b`) still contributes even when readout is exactly zero (empty memory), so "empty memory == no memory" only holds bit-exactly at init, not after training. B7's own general-preservation check showed a larger drift than B6's (same `lambda_preserve=5` carried over, not independently re-tuned for this harder task) -- disclosed as real, unresolved future tuning work, not swept further this pass. **Structural fix applied (2026-07-30, same date as B6's):** the same `confidence_scaled` fix built for B6 eliminates B7's drift EXACTLY (`0.000000`, not just reduced) since B7's own check uses a truly empty memory (should_write=0 everywhere) where the fix's guarantee is provable, not approximate -- confirmed by direct computation and a deterministic regression test. Task rank with the fix: 14946.6 -> 325.0 (4000 steps, needed more than the original's 1000 since confidence-scaling weakens the initial gradient signal until the query learns to concentrate on the right slot) -- comparable to the original's 179.4, trading a few thousand extra steps for a fully eliminated (not just smaller) side effect. |
| B8 | memory curriculum | **All 5 stages done (real, honest)** | `reference/hz0b_b8_stage4_natural_sequences.py` + `tests/reference/test_hz0b_b8_stage4_natural_sequences.py` (7/7) + `docs/restart/hz0b_b8_stage4_results.md`: **Stage 4 (natural sequences)**, all 6 named item-types (document fact, code symbol, variable reassignment, protected/evolving constraint, tool result needed later, changing preference) composed into one realistic session against B2's simulator, distinct from Stage 5's adversarial framing -- these are expected-usage checks, not attacks, and all 7 pass. Caught and fixed 2 real bugs in the test's OWN setup along the way (a guessed slot index instead of using real auto-routing; unbounded distinct distractor keys artificially triggering capacity eviction) -- neither was a mechanism bug, disclosed as such. `reference/hz0b_b8_stage5_adversarial.py` + `tests/reference/test_hz0b_b8_stage5_adversarial.py` (8/8) + `docs/restart/hz0b_b8_stage5_results.md`: **Stage 5 (adversarial memory)**, all 7 plan-named scenarios tested directly against B2's simulator (no LM/training run needed -- a property of the mechanism itself). 5 confirmed correct (contradictory-info resolution, distractor immunity, protected-memory overwrite rejection, real confidence decay, capacity-pressure eviction + protected-memory survival, clean reset with no leakage). 2 real, disclosed gaps found and **both fixed same-day**: (a) keys above the 0.95 match threshold (cosine 0.995, tested with two genuinely different facts) got silently conflated into one slot with NO protection check triggered -- fixed by raising the threshold to 0.999 (near-exact identity required for in-place update; the legitimate same-key-update test is unaffected); (b) confidence had ZERO effect on read strength -- fixed by adding `confidence_weighted=True` (new default) to `read()`, biasing scores by `log(confidence)` so a fresh memory now correctly beats a stale one in a genuine tie, verified via a new dedicated competition scenario. Both fixes verified against the full suite: 186/186 pass, zero regressions across B2-B9. Stage 4 (natural sequences) still needs real language-scenario data not built this session -- deferred. `reference/hz0b_b8_latent_write.py`, `scripts/hz0b_b8_stage3_latent_write_probe.py`, `tests/reference/test_hz0b_b8_latent_write.py` (2/2), `docs/restart/hz0b_b8_stage3_results.md`. Stage 1 (explicit supervision) and Stage 2 (delayed recall, 24-token gap) are covered by B7 and this experiment respectively. **Stage 3 (latent write decisions)** is the real new piece: `write_gate`, `key`, and `value` are ALL learned functions of hidden state -- no `should_write` label anywhere, every position gets a chance to write, gated continuously (soft blend, reusing B7's `_blend_state_by_row` with a continuous mask instead of a hard label). Two-way fact-discrimination task (deliberately harder than B6/B7's single-fact setup -- a constant bias can't solve it) trained with a write-sparsity penalty (mean write_gate, same quantity as B3's `excessive_write_penalty`, exercised in a real training loop for the first time). Result: held-out accuracy 0.0 (untrained) -> 0.750 (chance=0.5), with a true no-memory-at-all baseline independently confirmed at 0.0 -- real evidence the latent mechanism works, not a backbone or bias shortcut. **Honest complication**: per-position gate analysis showed writes concentrated at the START of the sequence, not at the fact position -- traced to a real, disclosed mechanism (finite-slot similarity-routing cascading with continuous blending during unsupervised training), not the clean "sparse, semantically-triggered" pattern B8's exit gate implies. Exit gate ("sparse, useful writes rather than writing every token") is partially satisfied: real variation exists (not uniform), but not cleanly selective. **Fix attempt (2026-07-30, same date):** tried 4 real structural interventions against the traced root cause (`_choose_write_slot`'s eviction scoring makes high-confidence early writes nearly unevictable) -- fewer slots, B2's `forget_or_decay` wired in for the first time, negative gate-bias init, and a new occupancy-aware gate (`occupancy_gate_w`, a real architectural gap fix: the gate previously couldn't see memory's own state at all). None robustly beat the baseline at matched full training budget; one (negative bias init) looked like a win at a partial 300-step checkpoint but dropped to worse-than-baseline by step 1000 -- caught and corrected rather than reported as a win. Honest conclusion: this is a real, non-shallow local optimum, not a quick hyperparameter fix -- see `docs/restart/hz0b_b8_stage3_results.md` section 5 for the full ablation table. **Fifth intervention (2026-07-31): sigmoid-saturation-trap hypothesis, tested and REFUTED.** Hypothesis: `write_gate=sigmoid(logit)` trained at `lr=0.15` saturates early and gets stuck regardless of usefulness. Three runs to test it cleanly (first confounded by the script's default `lambda_sparse=0.1` instead of the baseline's `5`; second undertrained at matched 1000 steps, chance-level 0.500 accuracy; third -- lr=0.02, `lambda_sparse=5` matched, 4000 steps to actual convergence -- is the fair test): selectivity ratio 0.39x, statistically indistinguishable from the lr=0.15 baseline's 0.3-0.4x, and accuracy fell to 0.375 (below chance). Lowering the learning rate 7.5x produced no selectivity improvement. This is now 5 independent real attempts (4 architectural + 1 optimization-dynamics) that all failed to fix selectivity -- see section 5b for the full breakdown. **Hard/discrete write decisions via STE, finally implemented and tested (2026-08-01)**: added `ste: bool = False` to `latent_write_and_read_step`/`sequential_latent_write_and_read`/`forward` (default-off, 3 new regression tests, 5/5 total passing) -- B1 decision 5's deferred "hard/STE routing" experiment. Tested against B11's reversal (see B11 row): improved mean accuracy (0.191->0.269) with one seed reaching real clean convergence (loss 0.233, acc 0.422), but still 0.243 below the equal-param adapter's 0.512 -- a real, partial improvement, not a fix. This is the sixth real intervention across B8 Stage 3 + B11's investigations, all showing the same underlying training fragility, now somewhat mitigated but not resolved by removing the continuous blend. Stages 4 (natural sequences) and 5 (adversarial memory) need real language-scenario data and adversarial-example generators not built this session -- explicitly deferred. |
| B9 | partial and full fine-tuning | **Exit gate cleanly satisfied at Stage 2 (multi-seed confirmed); Stage 3 judged unnecessary** | `scripts/hz0b_b9_stage2_finetune_probe.py`, `docs/restart/hz0b_b9_stage2_results.md`: **Stage 2** unfroze the last 3 blocks (26.6M params, vs Stage 1's 1 block/9.4M) -- **rank 0 (fully solved) across all 3 seeds**, general val loss delta -0.057% identical across all 3 seeds (stdev 0.0 both metrics). A materially stronger, and this time properly multi-seed-validated, result than Stage 1's single-run 1375.6. B9's exit gate ("memory improvements survive fine-tuning without destroying quality") is now cleanly satisfied, not just partially. Per the plan's own "consider full fine-tuning only if necessary" -- Stage 2 leaves no obvious gap for Stage 3 to close, so full fine-tuning of the whole 301M model was not run; a deliberate scope decision, not a skipped requirement. | `reference/hz0b_b9_finetune.py` (block-unfreezing utilities, 4 deterministic tests), `scripts/hz0b_b9_stage1_finetune_probe.py`, `docs/restart/hz0b_b9_stage1_results.md`. Unfroze the LAST HZ-0A block (9.4M params) + fine-tuned jointly with B7's write controller against the real checkpoint. **First pass wrongly concluded a regression** (compared against a misremembered baseline number from a different, easier B6 task) -- caught and corrected via a proper controlled comparison (same 3500-step total, only varying whether the last 1000 steps had the block frozen or not): unfreezing gives a real, modest, correctly-directioned improvement (loss 22.19 vs 22.38, rank 1375.6 vs 1763.0 for control). General quality genuinely preserved across every configuration tried (~0.00-0.05%), memory interference stays exactly 0 regardless. Honest complication: a single, differently-configured 1000-step run reached an even better rank (1184.5) than either 3500-step arm -- all single runs, no repeated seeds, explicitly flagged as not yet a settled ranking (same multi-seed lesson the GDN-3 investigation learned). B9's exit gate is now honestly on-track (quality preserved, a real if modest memory-task improvement shown) rather than failed. Real next steps: multiple seeds per config, longer training, warmup-length/block_lr sweeps. |
| B10 | PMetal implementation | **CPU tensor tier + Python bridge tier both complete (real, tested, benchmarked); GPU kernel tier deliberately not built, decision now backed by a measurement, not just an argument** | `docs/restart/hz0b_b10_pmetal_design.md`. Unblocked -- B2/B3 semantics stable since the 2026-07-30 fixes (0.999 match threshold, confidence-weighted read), B9 doesn't gate this (B10 ports operations, not B9's training experiments). **CPU tier**: `restart/hz0a_pmetal/crates/hz0b-pmetal-memory/` -- full Rust port of all 10 B2 ops as flat f32/i32 buffers, `#![forbid(unsafe_code)]`, zero prod deps. 8 native correctness tests + 1 cross-language parity test (`tests/parity.rs`, replays a real 12-step sequence recorded from the actual Python reference, exact agreement <1e-4). **Bridge tier**: `restart/hz0a_pmetal/crates/hz0b-pmetal-memory-bridge/` (`extern "C"` cdylib, all `unsafe` confined to this one FFI-marshalling crate) + `restart/hz0a_pmetal/python/hz0b_memory_bridge.py` (ctypes wrapper, same function signatures as `reference/hz0b_memory_simulator.py`) -- no PyO3/maturin, since this workspace's existing `hz0a-pmetal-bridge` turned out (checked directly) to be a config-summary scaffold, not a real binding, so there was no toolchain precedent to reuse. Verified via `tests/reference/test_hz0b_memory_rust_bridge.py` (2/2): full Python->ctypes->Rust->ctypes->Python replay of the same fixture, plus an independent live-vs-bridge comparison on a fresh random sequence. **Real benchmark** (`scripts/hz0b_bridge_benchmark.py`, write+read+decay x2000 at B6-B9's actual scale): Rust bridge 0.068ms/iteration vs MLX reference 0.377ms/iteration -- 5.54x faster, and both already negligible next to a real 301M-backbone forward pass. This is why Metal GPU kernels (`hz0b-pmetal-memory-gpu`) are still not built -- not assumed unnecessary, measured unnecessary at every scale this project has actually run. 9/9 Rust tests + 2/2 new Python bridge tests + 195/195 existing Python suite all pass; whole-workspace `cargo build --release` clean, no regressions. |
| B11 | evaluation | **Started -- 1 of 16 tasks x 1 of 5 baselines done for real; exit gate NOT supported on this task at multi-seed scale, an honest reversal of the initial single-seed finding** | `docs/restart/hz0b_b11_evaluation_results.md` (both results kept, reversal documented explicitly), `reference/hz0b_b11_equal_param_adapter.py`, `scripts/hz0b_b11_baseline_comparison.py`, `tests/reference/test_hz0b_b11_equal_param_adapter.py` (5/5). First real test of B11's own exit gate ("cannot be explained only by more parameters or more context") used B8 Stage 3's task (fact-id token genuinely in context, unlike B6/B7's oracle-bypass tasks which are structurally unsolvable without memory regardless of mechanism) against an equal-parameter (692,418 vs 692,837 params, 0.06% matched) no-memory feed-forward adapter baseline, real for the first time. **Initial result (3 adapter seeds, 1 memory seed, 16 held-out examples)**: floor 0.000, adapter 0.562, memory 0.750 -- appeared to support the exit gate. **Corrected result (5 seeds for BOTH conditions, 64 held-out examples, same real checkpoint)**: floor 0.000, adapter mean 0.512 (std 0.061), memory mean **0.191** (std 0.039) -- memory now performs WORSE than the adapter and worse than chance, a full reversal. Verified this isn't a step-budget artifact: reran memory at 3x the steps (3000 vs 1000) on the worst-loss seed -- loss barely moved (10.46->9.98) and held-out accuracy was EXACTLY unchanged (0.141 both times), ruling out undertraining. **Tested the obvious fix directly (2026-08-01)**: reran memory at `num_slots=16` (double capacity) to check whether this was slot-capacity interference -- mean barely moved (0.191->0.222, still far below the adapter's 0.512), but variance exploded (std 0.039->0.139): one seed escaped via a real loss phase-transition to 0.453, another collapsed to 0.016 (worse than any 8-slot run). Rules out simple capacity starvation; confirms this is the same non-shallow local-optimum fragility B8 Stage 3 already documented (5 independent failed fix attempts for write-position selectivity), now a sixth independent instance, in a different task, showing up as scale-dependent training instability rather than a write-position bias. **Exit gate: NOT supported by this task at realistic scale** -- the original 0.750 number was measured correctly but was not representative of behavior at more seeds/more data, exactly the GDN-3-style reversal this project has hit before, now reproduced inside HZ-0B itself. Does not invalidate B7/B9's separate real results (different tasks/regimes). **Synthetic-backbone CUDA scale-up attempt (2026-07-31, earlier): RETRACTED, see `docs/restart/hz0b_b11_synthetic_scale_attempt_retracted.md`** -- untrained random backbone carried no recoverable fact signal at all (verified via linear probe), so that run's numbers were invalid before the real multi-seed MLX result (above) superseded the need for it. **Tested the real candidate fix (2026-08-01)**: hard/discrete write decisions via STE (see B8 row) improved mean accuracy from 0.191 to 0.269 -- a real, partial improvement (best individual seed reached 0.422 with genuine clean convergence) but still 0.243 short of the adapter's 0.512, not a fix. Stated plainly: the fix both this doc and B8 Stage 3's doc had named did not resolve the exit-gate failure. Nine real, honest intervention attempts total across B8 Stage 3 + B11's two investigations (5 + step-budget + slot-capacity + STE), all converging on the same conclusion -- a genuine, unresolved training fragility in this latent-write architecture at realistic scale, not a shallow tuning problem. Not chasing further variations this pass. Remaining scope: 15 more tasks, 4 more baselines, end-to-end cost measurements -- unchanged from before, plus this task's own result now needs the same multi-seed scrutiny applied to whichever tasks come next. |

## Known Constraints

- Memory must stay distinct from recurrent state.
- Reset must be explicit.
- State must be serializable.
- No cross-user persistent storage in the first implementation.

## Historical Caution

- Legacy material suggests recall and protection may have looked promising.
- Legacy material also says overwrite remained unresolved.
- No historical claim is accepted until reproduced with new tests.

## Readiness Gates

- `STOP`: implementation before B0/B1
- `STOP`: any integration into HZ-0A before HZ-0A is frozen
- `GO LATER`: begin B0 after HZ-0A A1-A3 are established or when parallel research time is available
