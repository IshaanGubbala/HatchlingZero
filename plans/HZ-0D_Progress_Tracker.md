# HZ-0D Progress Tracker

Updated: August 4, 2026 (D0 through D7 complete: real prior work audited, the fast-weight contract specified and implemented, the isolated simulator demonstrates real few-shot mapping learning with exact snapshot/rollback/reset, a real 4-way update-mechanism comparison -- now through a v4 revision -- selects adaptive-ridge ALS delta prediction as the bounded method beating gradient descent on both clean-data quality and label-noise robustness while running ~150x faster, a real 8-baseline fair-comparison confirms the gains are attributable to session-local adaptation specifically, the HZ-0C dependency gate has been directly exercised and passes, D1's contract placement is wired into the REAL frozen checkpoint with bit-identical inactive reproduction and real active-adaptation gains, and D7 composes that wiring with HZ-0B's real memory read+write into one deterministic, feedback-loop-free per-token pipeline with a bounded fast-weight-update step)

## Mission

Add bounded session-local low-rank fast weights to a completed HZ-0C model, with strict isolation, snapshot, rollback, reset, and serialization semantics. Permanent pretrained weights must never change during ordinary use.

## Current Status

- Overall phase: `D0-D7 complete; D8 (curriculum) is next`
- Dependency status: HZ-0C is COMPLETE (`plans/HZ-0C_Progress_Tracker.md`) -- D5's dependency gate has been directly exercised and passes; D6 integration and D7 state ordering against the real checkpoint are both done
- Last verified HZ-0D evidence: `docs/restart/hz0d_d7_state_ordering_results.md` (2026-08-04) -- composed D6's fast-weight-augmented conditional attention with HZ-0B's real memory read+write into one deterministic per-token pipeline (`reference/hz0d_d7_state_ordering.py`), and added the one piece D6 didn't have: an explicit, bounded, opt-in fast-weight-update step (step 7). Verified directly on the real checkpoint: the full pipeline is deterministic (bit-identical across two runs); a real fast-weight update touches EXACTLY the named layer, all 5 others stay bit-exact zero; a call WITH an update produces logits bit-identical to an update-free call (no feedback loop -- the update cannot retroactively affect its own call's output); memory write is structurally one value per token position. Disclosed scope limit: `sequential_latent_write_and_read` starts every call from a fresh memory bank (no initial-state parameter in its real signature), so cross-call memory persistence is not implemented or claimed here. Prior verified evidence: `docs/restart/hz0d_d6_frozen_backbone_integration_results.md` (2026-08-04) -- D1's contract placement (anchor-attention output projection, 6 real `ATTENTION_INDICES` layers, `rank=16`/`dim=768`) wired into the REAL frozen checkpoint via `reference/hz0d_d6_integration.py`, reusing HZ-0C's own `masked_anchor_attention` pattern rather than modifying `hz0a_mlx_model.py`. Inactive fast weights reproduce HZ-0C's real `conditional_forward` BIT-IDENTICALLY (`mx.array_equal`, max abs diff 0.0) on real checkpoint logits. The wiring never mutates any real model parameter (checked bit-exactly). Active fast weights (D3's selected delta-prediction v4) measurably improve held-out loss on a real-weight few-shot task at the contract's real scale: 31.9% reduction at `k_train=256`, growing to 99.1% at `k_train=1024`. Real, disclosed scaling finding: gradient descent's D2/D3 toy-scale learning rate (`0.02`) gives ~0% improvement at `dim=768` until retuned (`lr=3.0`), at which point it tracks delta prediction closely -- delta prediction needs no such retuning. Prior verified evidence: `docs/restart/hz0d_d5_dependency_gate_results.md` (2026-08-04) -- all 6 named D5 prerequisites checked directly this session with real evidence: frozen HZ-0C (all 9 phases complete), stable HZ-0B memory (9/10 completion criteria unconditionally met per `hz0b_completion_verdict.md`), surprise controller (C7 fixed, real), triggered attention (C1/C6, real, memory-wired), PMetal implementation (C8, real Rust/Metal + FFI, tested), trained checkpoint (loaded and run directly this session -- step 38403, 100,002,816 tokens seen, 1,124 real weight arrays, finite forward-pass output, parameter count cross-checked exactly against `hz0c_c1_topology.md`'s audited 301,178,112, now also locked in as a regression test in `tests/reference/test_hz0d_d5_dependency_gate.py`), baselines (HZ-0C C4 + HZ-0D D4). Prior verified evidence: `docs/restart/hz0d_d4_fair_baselines_results.md` (2026-08-04) -- real 8-baseline comparison (no adaptation, in-context attention, longer unadapted context at 2 sizes, k-NN retrieval at 2 k-values, static random adapter, permanent meta-trained LoRA, plus D3's gradient descent and delta prediction) on the SAME D2 task, same 8 seeds as D3. Every baseline is at least 3x worse than either real fast-weight mechanism (gd=0.3997, delta_v4=0.3699); static random adapter ties no-adaptation exactly (extra capacity alone buys nothing); permanent meta-LoRA is WORSE than no-adaptation (a shared adapter across independent random rules has nothing systematic to learn); longer unadapted context saturates ~3x worse even at 17x more examples. D4 exit gate met directly, not assumed. Prior verified evidence: `docs/restart/hz0d_d3_update_mechanism_results.md` (2026-08-04, revised through v4) -- real 4-way comparison (gradient descent, Hebbian/delta-rule, closed-form delta prediction, error-conditioned gradient descent) on the SAME D2 task and shared clipping. Delta prediction originally collapsed under label noise (~135x worse than gradient descent, exact-interpolation overfitting) -- fixed across three iterations: ridge regularization (v2, statistically comparable but only one axis at a time), alternating least squares (v3, within ~3% of gradient descent on BOTH axes), then per-task adaptive ridge via a real spectral noise estimator (v4, `estimate_noise_ratio`) which now BEATS gradient descent on both clean-data quality (7.4%) and noise robustness (14.8%) simultaneously, ~150x faster. Hebbian is capacity-limited (half the parameters frozen, held-out loss plateaus 1.07-1.22 regardless of tuning, confirmed via a 12-configuration sweep); error-conditioned gating is slightly worse and slower than plain gradient descent here. Adaptive-ridge ALS delta prediction (v4) is now the selected bounded method; gradient descent remains implemented and tested as a fallback, not deleted. **Real, tested limitation found and disclosed** (not just theorized): v4 loses to gradient descent when the task's true rule is not exactly rank-`config.rank` (`estimate_noise_ratio` cannot tell "spectral mass from label noise" apart from "spectral mass from genuinely higher rank" -- both inflate the same ratio); a leave-one-out predictability discriminator was tried and found too high-variance at `k_train=6` to fix this. Locked in as a regression test (`test_adaptive_ridge_delta_prediction_loses_to_gradient_descent_under_rank_misspecification`); guardrail before D6: measure real HZ-0C deltas' effective rank before trusting v4 unaudited.
- Current stopping rule: do not begin curriculum work (D8) before state ordering is deterministic and unambiguous -- **met**

## Phase Tracker

| Phase | Deliverable | Status | Evidence / Notes |
| --- | --- | --- | --- |
| D0 | history audit and recovered requirements | **Complete** | `docs/restart/hz0d_history_audit.md`, `hz0d_recovered_requirements.md` -- real `git log --all` sweep found substantial prior work (the old "Phase 16" HZ-0C-named fast weights, 6 commits, archived at `archive/src/hz0/fast_weights/`). Snapshot/rollback/session-management design is real and reusable as a pattern. The "gradient-based" adaptation mechanism was NOT real gradient descent -- verified by reading the actual update code, which discards the one signal (`loss_pert`) that would make it a valid finite-difference estimator and instead applies unbiased random noise. This was self-admitted in the phase's own commit message ("insufficient for learning") but the following phase declared "production-ready" anyway; both facts are disclosed. |
| D1 | fast-weight contract | **Complete** | `docs/restart/hz0d_d1_contract.md`, `reference/hz0d_fast_weights.py`, `tests/reference/test_hz0d_fast_weights.py` (15 tests, all real code not stubs). Layers: anchor-attention output projection at the 6 existing HZ-0C `ATTENTION_INDICES`. Rank 16 at dim 768 (147,456 params / 576 KiB per session, audited exactly, not estimated). Update: real `mx.grad`-based gradient descent, verified against finite differences to `<1e-2` and shown to strictly reduce loss on both a single step and a 200-step toy-mapping convergence test (>50% loss reduction). Clipping: realized-delta Frobenius norm bounded regardless of input gradient magnitude. Decay: multiplicative, `1.0` exact no-op, tested. Snapshot/rollback/reset: bit-identical (`mx.array_equal`), not approximate. Serialization: exact round-trip. A real bug (symmetric zero-init gives exactly-zero gradients for both factors, a dead saddle point) was found BY the test suite and fixed with the standard asymmetric LoRA init, documented in the contract doc as a real example of D0's lesson applied. |
| D2 | isolated simulator | **Complete** | `reference/hz0d_isolated_simulator.py`, `tests/reference/test_hz0d_isolated_simulator.py` (8 tests), `docs/restart/hz0d_d2_isolated_simulator_results.md`. Real few-shot symbol-remapping task (fixed low-rank rule, train/held-out symbol split); a real calibration finding (dim=32/rank=4/k_train=4 gave pure memorization, 5.9% held-out improvement -- fixed by right-sizing to dim=8/rank=2/k_train=6, verified across 10 seeds: 30-98% held-out loss reduction, always positive) is documented, not hidden. Adaptation speed measured across step checkpoints. Contradictory-rule-change interference measured directly (rule 1's held-out loss rises from 0.244 to 0.816 after switching to rule 2). Decay shown monotonic in both state norm and task loss. Snapshot/rollback and reset verified BOTH bit-identical (`mx.array_equal`) AND behaviorally exact (held-out task loss equal, not just tensors) under real interference. Noisy updates stay finite/bounded through 400 steps; a malicious 1e6-magnitude gradient is clipped exactly and the state remains fully recoverable via rollback/reset afterward. |
| D3 | update-mechanism selection | **Complete, including three same-day fixes** | `reference/hz0d_update_mechanisms.py`, `tests/reference/test_hz0d_update_mechanisms.py` (12 tests), `docs/restart/hz0d_d3_update_mechanism_results.md`. All 4 plan-named candidates implemented and compared on the identical D2 task with shared clipping. Original finding: delta prediction (closed-form least-squares + SVD truncation) won clean data (~4,000x faster, best quality) but collapsed under label noise (~135x worse than gradient descent, exact-interpolation overfitting). **Fix 1**: ridge regularization on the normal equations closed the gap to statistically comparable, but only on one axis at a time. **Fix 2 ("get ridge regularized to gradient accuracy")**: replaced solve-then-truncate with ridge-regularized ALTERNATING LEAST SQUARES (ALS) directly on the rank-2 factors, verified via a noise-free synthetic sanity check first (recovers a known rank-2 matrix to <0.004 error); swept ridge to `ridge=0.27`, landing within +2.8% (clean) and +2.7% (noisy) of gradient descent simultaneously, ~480x faster. **Fix 3 ("no get ridge regularized BETTER than gradient descent, especially bc its much faster")**: replaced the single fixed ridge with a real per-task adaptive ridge, `estimate_noise_ratio(task, config)` -- the ratio of a lightly-regularized delta's singular-value mass outside the task's true rank to mass inside it, which separates clean from noisy data by ~3 orders of magnitude (verified across 5 seeds) since the task's true rule is exactly rank-`config.rank` by construction. LOOCV and GCV were tried first and rejected (both too high-variance at `k_train=6`, picking erratic ridge values seed to seed) before landing on this structurally-motivated estimator. `base_ridge=0.03, ridge_scale=1.2`: clean=0.3703 (7.4% BETTER than GD's 0.3997), noisy=0.7571 (14.8% BETTER than GD's 0.8887), simultaneously, ~150x faster -- confirmed at both the 8-seed mean and a single-seed spot check (seed=1: clean 0.1045<0.1235, noisy 0.3309<0.3356). Hebbian's capacity limit (a_fast held fixed) is real, confirmed via a 12-config sweep, not fixable the same way. **Adaptive-ridge ALS delta prediction is now the selected mechanism**, beating gradient descent outright rather than merely matching it; gradient descent remains fully implemented and tested as a fallback. |
| D4 | fair adaptation baselines | **Complete** | `reference/hz0d_fair_baselines.py`, `tests/reference/test_hz0d_fair_baselines.py` (5 tests), `docs/restart/hz0d_d4_fair_baselines_results.md`. 8 baselines on the identical D2 task/seeds: no adaptation (1.4120), static random adapter (1.4122, ties no-adaptation exactly), permanent meta-LoRA (1.7818, WORSE than no-adaptation), k-NN retrieval k=1/k=3 (1.9959/1.5576), in-context attention (1.2611), longer context +24/+100 examples (1.1187/1.1619, saturates). Real fast-weight adaptation: gradient descent (0.3997), delta prediction v4 (0.3699) -- both beat every baseline by >=3x. Two plan-named baselines ("HZ-0B memory only", "HZ-0C only") have no faithful analog without a real token sequence; substituted with the mechanically closest generic analogs (k-NN retrieval, in-context attention respectively) and disclosed as a substitution, not the real thing -- real HZ-0B/HZ-0C-specific baselines belong at D6. |
| D5 | HZ-0C dependency gate | **Complete -- gate exercised and passed** | `docs/restart/hz0d_d5_dependency_gate_results.md`, `tests/reference/test_hz0d_d5_dependency_gate.py` (2 tests). All 6 named prerequisites (frozen HZ-0C, stable HZ-0B memory, surprise controller, triggered attention, PMetal implementation, trained checkpoint, baselines) checked directly with real evidence, not re-cited: the trained checkpoint was loaded and run this session (real forward pass, finite output, exact 301,178,112-parameter cross-check against `hz0c_c1_topology.md`), not just confirmed to exist on disk. |
| D6 | frozen-backbone integration | **Complete** | `reference/hz0d_d6_integration.py`, `tests/reference/test_hz0d_d6_integration.py` (4 tests), `docs/restart/hz0d_d6_frozen_backbone_integration_results.md`. Wired D1's contract placement into the REAL frozen checkpoint via HZ-0C's own established custom-forward pattern, not by modifying `hz0a_mlx_model.py`. Inactive fast weights reproduce `conditional_forward` bit-identically (real checkpoint logits, `mx.array_equal`, 0.0 max diff); wiring never mutates any real model parameter (checked bit-exactly); active fast weights (D3's v4) measurably improve held-out loss at the contract's real scale (31.9% to 99.1% reduction as `k_train` grows 256->1024), using the REAL frozen output-projection weight as the task's base, not a synthetic one. |
| D7 | state ordering | **Complete** | `reference/hz0d_d7_state_ordering.py`, `tests/reference/test_hz0d_d7_state_ordering.py` (6 tests), `docs/restart/hz0d_d7_state_ordering_results.md`. Composes D6's fast-weight conditional attention with HZ-0B's real memory read+write and adds the previously-missing bounded fast-weight-update step (step 7). Verified on the real checkpoint: full-pipeline determinism (bit-identical across two runs); a fast update touches exactly the named layer (all others bit-exact zero); no feedback loop (a with-update call's logits are bit-identical to an update-free call); memory write structurally one-per-position. Disclosed limit: cross-call memory persistence is not implemented (`sequential_latent_write_and_read` takes no initial state) -- named as future work, not assumed solved. |
| D8 | curriculum | Not started | Supervised updates through natural schemas and adversarial rollback tasks |
| D9 | PMetal implementation | Not started | Apply/update/decay/snapshot/rollback/reset and batched deterministic sessions |
| D10 | evaluation | Not started | Adaptation, retention, interference, fidelity, overhead, degradation, resistance |

## Required Artifacts

- [x] `docs/restart/hz0d_history_audit.md`
- [x] `docs/restart/hz0d_recovered_requirements.md`
- [x] Fast-state tensor/lifecycle contract (`docs/restart/hz0d_d1_contract.md`, `reference/hz0d_fast_weights.py`)
- [x] Standalone low-rank simulator and exact lifecycle tests (`reference/hz0d_isolated_simulator.py`, `tests/reference/test_hz0d_isolated_simulator.py`)
- [x] Update-mechanism comparison report (`docs/restart/hz0d_d3_update_mechanism_results.md`)
- [x] Fair adaptation baseline report (`docs/restart/hz0d_d4_fair_baselines_results.md`)
- [x] Frozen-backbone integration report (`docs/restart/hz0d_d6_frozen_backbone_integration_results.md`)
- [x] State-ordering tests (`docs/restart/hz0d_d7_state_ordering_results.md`)
- [ ] PMetal parity and gradient report
- [ ] Full adaptation, overhead, and safety report

## Contract Checklist

- [x] Define `W_effective = W_base + A_fast @ B_fast` placement and rank. -- anchor-attention output projection, 6 layers, rank 16
- [x] Define update frequency, decay, clipping, normalization, and per-session budget. -- `max_updates_per_session=50` (policy field), multiplicative decay, delta-norm clipping, no normalization beyond clipping (documented as a deliberate simplicity choice)
- [x] Prove permanent weights remain unchanged. -- `base_weight`/`base_bias` are plain arrays with no gradient path from `(a_fast, b_fast)`, tested directly (`test_permanent_weight_never_appears_in_fast_weight_gradient`)
- [x] Prove snapshot/rollback and reset fidelity exactly. -- `mx.array_equal`, not approximate closeness
- [x] Define serialization, batching, deterministic ordering, and gradient flow. -- exact round-trip serialization; deterministic seeded init; gradient flow verified via finite differences
- [x] Bound maximum fast-state memory and update cost. -- `fast_state_memory_bytes` computed from real shapes, tested against the hand-computed default (589,824 bytes)

## Exit Gates

- [x] D0: mechanism is specified independently of archived code. -- `docs/restart/hz0d_history_audit.md`, `hz0d_recovered_requirements.md`
- [x] D1: every state tensor and lifecycle operation is documented. -- `docs/restart/hz0d_d1_contract.md` (prose) + `reference/hz0d_fast_weights.py` (field-level docstrings) + `tests/reference/test_hz0d_fast_weights.py` (15 tests, every operation independently verified, not just documented)
- [x] D2: temporary mappings work and prior state is restored exactly. -- `docs/restart/hz0d_d2_isolated_simulator_results.md`: 73.7% mean held-out loss reduction across 8 seeds (always positive), snapshot/rollback and reset verified both bit-identical and behaviorally exact under real interference
- [x] D3: one bounded update method clearly beats simple alternatives. -- `docs/restart/hz0d_d3_update_mechanism_results.md` (v4): adaptive-ridge ALS delta prediction beats Hebbian on quality (real capacity limit, tuning-sweep-confirmed), beats error-conditioned gradient descent (worse and slower), and now beats plain gradient descent itself on both clean-data quality (7.4%) and label-noise robustness (14.8%) simultaneously, at ~150x lower cost -- selected as the bounded method outright, not just "not disqualified"
- [x] D4: gains are attributable to temporary fast adaptation. -- `docs/restart/hz0d_d4_fair_baselines_results.md`: no baseline (no adaptation, in-context attention, longer context, k-NN retrieval, static random adapter, permanent meta-LoRA) gets within 3x of either real fast-weight mechanism; static-capacity and permanent-adapter explanations are directly ruled out, not just argued against
- [x] D5: HZ-0C dependency gate passes. -- `docs/restart/hz0d_d5_dependency_gate_results.md`: all 6 named prerequisites verified with real, current evidence
- [x] D6: inactive fast weights reproduce HZ-0C behavior; active weights improve adaptation. -- `docs/restart/hz0d_d6_frozen_backbone_integration_results.md`: bit-identical inactive reproduction on the real checkpoint; 31.9%-99.1% real held-out loss reduction from active adaptation at contract scale
- [x] D7: state transitions are deterministic and unambiguous. -- `docs/restart/hz0d_d7_state_ordering_results.md`: bit-identical determinism across repeated runs, exactly-one-layer updates, verified absence of feedback loops
- [ ] D9: PMetal gradients match the reference.
- [ ] D10: fast adaptation beats fair baselines while respecting all bounds.

## First Milestone Checklist

- [x] Implement a standalone low-rank fast-weight simulator. -- `reference/hz0d_isolated_simulator.py`
- [x] Learn a temporary mapping from a few examples. -- 73.7% mean held-out loss reduction across 8 seeds, `k_train=6`
- [x] Snapshot, introduce interference, and roll back exactly. -- bit-identical AND behaviorally exact (held-out loss equal) after real interference
- [x] Reset to the baseline state exactly. -- bit-identical to fresh init, behaviorally exact task loss
- [x] Report adaptation speed, state norm, interference, rollback, and reset metrics. -- `docs/restart/hz0d_d2_isolated_simulator_results.md`

**First milestone met.**
